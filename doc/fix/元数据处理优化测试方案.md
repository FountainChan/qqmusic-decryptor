# 元数据处理优化测试方案

**创建时间**：2026-01-30  
**版本**：v1.0  
**状态**：📋 待确认  
**类型**：测试方案（中文）

---

## 🎯 测试目标

### 主要目标

1. ✅ **验证音轨号写入功能**：确认音轨号能正确写入到音频文件
2. ✅ **验证年份和封面写入功能**：确认年份和封面能正确写入到音频文件
3. ✅ **验证 API 调用异常处理**：确认 API 调用异常能被正确捕获和处理
4. ✅ **验证跳过逻辑**：确认当找不到 `albummid` 时能正确跳过年份和封面处理
5. ✅ **验证独立处理逻辑**：确认音轨号、年份、封面能独立处理

---

## 🧪 测试计划

### 测试 1：音轨号写入验证（FLAC 和 OGG）

**目的**：验证音轨号写入功能是否正确

**测试文件**：
- ✅ `01 B_O_K.ogg` → 预期：音轨号 `1`，成功
- ✅ `02 无言无语.ogg` → 预期：音轨号 `2`，成功
- ✅ `03 好人 (Radio Edit).ogg` → 预期：音轨号 `3`，成功
- ✅ `04 Erica.ogg` → 预期：音轨号 `4`，成功
- ✅ `05 情歌.flac` → 预期：音轨号 `5`，成功
- ✅ `06 男人 KTV.ogg` → 预期：音轨号 `6`，成功
- ✅ `07 一句.ogg` → 预期：音轨号 `7`，成功
- ✅ `08 Volar.ogg` → 预期：音轨号 `8`，成功
- ✅ `09 未输.ogg` → 预期：音轨号 `9`，成功
- ✅ `10 头条新闻.ogg` → 预期：音轨号 `10`，成功
- ✅ `11 情永落.ogg` → 预期：音轨号 `11`，成功
- ✅ `12 我不是好人.ogg` → 预期：音轨号 `12`，成功
- ✅ `13 贝壳.ogg` → 预期：音轨号 `13`，成功
- ✅ `14 运.ogg` → 预期：音轨号 `14`，成功
- ✅ `歌名.flac` → 预期：音轨号 `None`，跳过

**测试方法**：
```bash
cd /d/WorkDev/qqmusic_decryptor
./run_supplement.sh "/g/QQMusic/Decrypted/VipSongsDownload/侧田/From Justin (Collection Of His First 3 Years)" 2>&1 | grep -E "音轨号|TRACKNUMBER|写入"
```

**预期日志**：
```
[INFO] 处理文件 1/15: 01 B_O_K.ogg
[INFO] 提取标签 - 01 B_O_K.ogg: Artist=侧田, Album=From Justin...
[INFO] 写入音轨号 (OGG): 1  ← 关键验证点
[OK] 音轨号: 1  ← 关键验证点
[INFO] 查询 API 获取专辑信息...
...
[OK] 01 B_O_K.ogg  ← 年份和封面处理成功
```

**验证要点**：
- [ ] 日志显示 `写入音轨号 (OGG): X`
- [ ] 日志显示 `[OK] 音轨号: X`
- [ ] 所有文件都有音轨号写入日志（除了文件名没有数字的文件）

---

### 测试 2：音轨号检查验证（避免重复写入）

**目的**：验证音轨号检查逻辑是否正确

**测试方法 1**：第二次运行脚本

```bash
cd /d/WorkDev/qqmusic_decryptor
./run_supplement.sh "/g/QQMusic/Decrypted/VipSongsDownload/侧田/From Justin (Collection Of His First 3 Years)" 2>&1 | grep -E "音轨号|已经有音轨号|跳过"
```

**预期日志**：
```
[INFO] 处理文件 1/15: 01 B_O_K.ogg
[INFO] OGG 文件已经有音轨号: ['1']，跳过写入  ← 关键验证点
[OK] 01 B_O_K.ogg
```

**验证要点**：
- [ ] 第二次运行显示 `OGG 文件已经有音轨号: X，跳过写入`
- [ ] 第三次运行也显示 `OGG 文件已经有音轨号: X，跳过写入`
- [ ] 没有显示 `写入音轨号 (OGG): X`（重复写入）

---

### 测试 3：年份和封面写入验证

**目的**：验证年份和封面写入功能是否正确

**测试文件**：
- ✅ `01 B_O_K.ogg` → 预期：年份 `2006`，封面 `52487 bytes`
- ✅ `05 情歌.flac` → 预期：年份 `2006`，封面 `52487 bytes`

**测试方法**：
```bash
cd /d/WorkDev/qqmusic_decryptor
./run_supplement.sh "/g/QQMusic/Decrypted/VipSongsDownload/侧田/From Justin (Collection Of His First 3 Years)" 2>&1 | grep -E "年份|封面|DATE|PICTURE|保存元数据"
```

**预期日志**：
```
[INFO] 找到 aDate: 2006-03-24  ← 关键验证点
[INFO] 下载封面: https://y.gtimg.cn/music/photo_new/T002R500x500M00000...jpg
[INFO] 封面数据大小: 52487 bytes  ← 关键验证点
[INFO] 处理文件 5/15: 05 情歌.flac
[INFO] 添加年份 (FLAC): 2006  ← 关键验证点
[INFO] 嵌入封面 (FLAC)  ← 关键验证点
[INFO] 保存元数据 (FLAC): 05 情歌.flac  ← 关键验证点
[OK] 05 情歌.flac
```

**验证要点**：
- [ ] 日志显示 `找到 aDate: YYYY-MM-DD`
- [ ] 日志显示 `封面数据大小: X bytes`
- [ ] 日志显示 `添加年份 (FLAC): YYYY`（或 `添加年份 (OGG): YYYY`）
- [ ] 日志显示 `嵌入封面 (FLAC)`（FLAC 文件）
- [ ] 日志显示 `保存元数据 (FLAC): 文件名`（或 `保存元数据 (OGG): 文件名`）

---

### 测试 4：API 调用异常处理验证

**目的**：验证 API 调用异常处理是否正确

**测试方法**：
```bash
cd /d/WorkDev/qqmusic_decryptor
# 测试 1：手动触发 API 调用异常
# 测试 2：模拟网络超时
# 测试 3：模拟 API 返回错误数据
```

**预期日志**（异常情况）：
```
[INFO] 查询 API 获取专辑信息...
[ERROR] 查询专辑信息异常: Connection timeout  ← 关键验证点
[WARNING] 跳过专辑：未找到发行年份  ← 关键验证点
...
==================================================
处理完成
==================================================

总文件数: 15
成功: 0  ← 正确：没有处理文件
失败: 0  ← 正确：没有处理失败
跳过: 0  ← 正确：专辑被跳过，不是文件被跳过
```

**验证要点**：
- [ ] 异常情况显示 `ERROR: 查询专辑信息异常: X`
- [ ] 异常情况显示 `WARNING: 跳过专辑：未找到发行年份`
- [ ] 异常情况下，专辑被跳过，不处理任何文件
- [ ] 统计信息显示 `总文件数: 15`，但 `成功: 0`，`跳过: 0`

---

### 测试 5：找不到 albummid 跳过验证

**目的**：验证找不到 `albummid` 时的跳过逻辑是否正确

**测试方法**：
```bash
cd /d/WorkDev/qqmusic_decryptor
./run_supplement.sh "/g/QQMusic/Decrypted/VipSongsDownload/中央芭蕾舞团管弦乐队" 2>&1 | grep -E "albummid|album|跳过|未找到|album信息"
```

**预期日志**：
```
找到 575 个 FLAC 文件
专辑信息: ['中央芭蕾舞团管弦乐队'] - ['']  ← 关键验证点
查询 API 获取专辑信息...
WARNING: 未找到 albummid  ← 关键验证点
WARNING: 跳过专辑：未找到发行年份  ← 关键验证点
...
==================================================
处理完成
==================================================

总文件数: 575  ← 正确：统计信息正确
成功: 0  ← 正确：没有处理文件
失败: 0  ← 正确：没有处理失败
跳过: 0  ← 正确：专辑被跳过，不是文件被跳过
```

**验证要点**：
- [ ] 日志显示 `专辑信息: ['...'] - ['']`
- [ ] 日志显示 `WARNING: 未找到 albummid`
- [ ] 日志显示 `WARNING: 跳过专辑：未找到发行年份`
- [ ] 统计信息显示 `总文件数: 575`，但 `成功: 0`，`跳过: 0`

---

### 测试 6：批量处理验证

**目的**：验证所有文件都能正确处理

**测试命令**：
```bash
cd /d/WorkDev/qqmusic_decryptor
./run_supplement.sh "/g/QQMusic/Decrypted/VipSongsDownload/侧田/From Justin (Collection Of His First 3 Years)" 2>&1 | tail -50
```

**预期结果**：
```
============================================================
处理完成
============================================================

总文件数: 15  ← 正确：统计信息正确
成功: 15  ← 正确：所有文件都正确处理
失败: 0  ← 正确：没有处理失败
跳过: 0  ← 正确：没有文件被跳过
============================================================
```

**验证要点**：
- [ ] 总文件数：15（1 FLAC + 14 OGG）
- [ ] 成功数：15（所有文件都正确处理）
- [ ] 失败数：0（没有处理失败）
- [ ] 跳过数：0（没有文件被跳过）

---

## 📋 验证清单

### 功能验证

- [ ] **音轨号写入功能**：所有文件的音轨号都能正确写入
- [ ] **音轨号检查功能**：已经有音轨号的文件不会被覆盖
- [ ] **年份和封面写入功能**：年份和封面都能正确写入
- [ ] **API 调用异常处理**：所有 API 调用异常都能被捕获
- [ ] **跳过逻辑**：找不到 `albummid` 时能正确跳过专辑处理

### 日志验证

- [ ] **日志显示**：所有操作都有详细日志输出
- [ ] **日志级别**：INFO 级别日志正确显示
- [ ] **日志格式**：日志格式清晰易懂
- [ ] **异常日志**：异常情况显示 ERROR 级别日志
- [ ] **跳过日志**：跳过情况显示 WARNING 级别日志

### 数据验证

- [ ] **元数据写入**：所有文件的元数据都正确写入
- [ ] **数据完整性**：元数据没有丢失或损坏
- [ ] **数据一致性**：元数据与文件名和 API 返回数据一致

---

## 🚀 测试执行计划

### 阶段 1：音轨号写入验证（计划 1）

**任务**：
- [ ] 运行测试 1：音轨号写入验证
- [ ] 验证所有文件的音轨号都能正确写入
- [ ] 记录测试结果

**预期结果**：
- ✅ 所有 15 个文件的音轨号都正确写入
- ✅ 所有文件都有 `[OK] 音轨号: X` 日志
- ✅ 1 个文件（文件名没有数字）显示 `[SKIP] 未找到音轨号`

---

### 阶段 2：音轨号检查验证（计划 2）

**任务**：
- [ ] 运行测试 2：第二次运行脚本
- [ ] 验证音轨号检查逻辑是否正确
- [ ] 验证是否有重复写入
- [ ] 记录测试结果

**预期结果**：
- ✅ 第二次运行显示 `OGG 文件已经有音轨号: X，跳过写入`
- ✅ 第三次运行也显示 `OGG 文件已经有音轨号: X，跳过写入`
- ✅ 没有显示 `写入音轨号 (OGG): X`（重复写入）

---

### 阶段 3：年份和封面写入验证（计划 3）

**任务**：
- [ ] 运行测试 3：年份和封面写入验证
- [ ] 验证年份和封面都能正确写入
- [ ] 验证 API 调用是否成功
- [ ] 记录测试结果

**预期结果**：
- ✅ 日志显示 `找到 aDate: YYYY-MM-DD`
- ✅ 日志显示 `封面数据大小: X bytes`
- ✅ 日志显示 `添加年份 (FLAC): YYYY`（或 `添加年份 (OGG): YYYY`）
- ✅ 日志显示 `嵌入封面 (FLAC)`（FLAC 文件）

---

### 阶段 4：API 调用异常处理验证（计划 4）

**任务**：
- [ ] 运行测试 4：模拟 API 调用异常
- [ ] 验证异常处理逻辑是否正确
- [ ] 验证跳过逻辑是否正确
- [ ] 记录测试结果

**预期结果**：
- ✅ 异常情况显示 `ERROR: 查询专辑信息异常: X`
- ✅ 异常情况显示 `WARNING: 跳过专辑：未找到发行年份`
- ✅ 异常情况下，专辑被跳过，不处理任何文件

---

### 阶段 5：找不到 albummid 跳过验证（计划 5）

**任务**：
- [ ] 运行测试 5：找不到 albummid 跳过验证
- [ ] 验证找不到 `albummid` 时的跳过逻辑
- [ ] 验证统计信息是否正确
- [ ] 记录测试结果

**预期结果**：
- ✅ 日志显示 `专辑信息: ['...'] - ['']`
- ✅ 日志显示 `WARNING: 未找到 albummid`
- ✅ 日志显示 `WARNING: 跳过专辑：未找到发行年份`
- ✅ 统计信息显示 `总文件数: 575`，但 `成功: 0`，`跳过: 0`

---

### 阶段 6：批量处理验证（计划 6）

**任务**：
- [ ] 运行测试 6：批量处理验证
- [ ] 验证所有文件都能正确处理
- [ ] 验证统计信息是否正确
- [ ] 记录测试结果

**预期结果**：
- ✅ 总文件数：15（1 FLAC + 14 OGG）
- ✅ 成功数：15（所有文件都正确处理）
- ✅ 失败数：0（没有处理失败）
- ✅ 跳过数：0（没有文件被跳过）

---

## 📊 测试结果记录

### 测试 1：音轨号写入验证

| 文件名 | 预期 | 实际 | 状态 |
|--------|------|------|------|
| `01 B_O_K.ogg` | 音轨号 `1` | - | ⏸️ 待测试 |
| `02 无言无语.ogg` | 音轨号 `2` | - | ⏸️ 待测试 |
| `03 好人 (Radio Edit).ogg` | 音轨号 `3` | - | ⏸️ 待测试 |
| `04 Erica.ogg` | 音轨号 `4` | - | ⏸️ 待测试 |
| `05 情歌.flac` | 音轨号 `5` | - | ⏸️ 待测试 |
| `06 男人 KTV.ogg` | 音轨号 `6` | - | ⏸️ 待测试 |
| `07 一句.ogg` | 音轨号 `7` | - | ⏸️ 待测试 |
| `08 Volar.ogg` | 音轨号 `8` | - | ⏸️ 待测试 |
| `09 未输.ogg` | 音轨号 `9` | - | ⏸️ 待测试 |
| `10 头条新闻.ogg` | 音轨号 `10` | - | ⏸️ 待测试 |
| `11 情永落.ogg` | 音轨号 `11` | - | ⏸️ 待测试 |
| `12 我不是好人.ogg` | 音轨号 `12` | - | ⏸️ 待测试 |
| `13 贝壳.ogg` | 音轨号 `13` | - | ⏸️ 待测试 |
| `14 运.ogg` | 音轨号 `14` | - | ⏸️ 待测试 |
| `歌名.flac` | 音轨号 `None` | - | ⏸️ 待测试 |

---

### 测试 2：音轨号检查验证

| 运行次数 | 预期日志 | 实际日志 | 状态 |
|---------|----------|----------|------|
| 第一次运行 | `写入音轨号 (OGG): 1` | - | ⏸️ 待测试 |
| 第二次运行 | `OGG 文件已经有音轨号: ['1']，跳过写入` | - | ⏸️ 待测试 |
| 第三次运行 | `OGG 文件已经有音轨号: ['1']，跳过写入` | - | ⏸️ 待测试 |

---

### 测试 3：年份和封面写入验证

| 文件名 | 预期年份 | 预期封面 | 实际年份 | 实际封面 | 状态 |
|--------|---------|---------|---------|---------|------|
| `01 B_O_K.ogg` | `2006` | `52487 bytes` | - | - | ⏸️ 待测试 |
| `05 情歌.flac` | `2006` | `52487 bytes` | - | - | ⏸️ 待测试 |

---

### 测试 4：API 调用异常处理验证

| 场景 | 预期日志 | 实际日志 | 状态 |
|------|----------|----------|------|
| 正常情况 | `找到 aDate: YYYY-MM-DD` | - | ⏸️ 待测试 |
| 异常情况（超时） | `ERROR: 查询专辑信息异常: Connection timeout` | - | ⏸️ 待测试 |
| 异常情况（错误） | `ERROR: 查询专辑信息异常: ...` | - | ⏸️ 待测试 |

---

### 测试 5：找不到 albummid 跳过验证

| 场景 | 预期日志 | 实际日志 | 状态 |
|------|----------|----------|------|
| 找不到 albummid | `WARNING: 未找到 albummid` | - | ⏸️ 待测试 |
| 找不到 aDate | `WARNING: 跳过专辑：未找到发行年份` | - | ⏸️ 待测试 |
| 统计信息 | `总文件数: 575`，`成功: 0`，`跳过: 0` | - | ⏸️ 待测试 |

---

### 测试 6：批量处理验证

| 指标 | 预期值 | 实际值 | 状态 |
|------|--------|--------|------|
| 总文件数 | `15` | - | ⏸️ 待测试 |
| 成功数 | `15` | - | ⏸️ 待测试 |
| 失败数 | `0` | - | ⏸️ 待测试 |
| 跳过数 | `0` | - | ⏸️ 待测试 |

---

## 🎯 成功标准

### 必须满足的条件

1. ✅ **音轨号写入功能**：所有文件的音轨号都能正确写入
2. ✅ **音轨号检查功能**：已经有音轨号的文件不会被覆盖
3. ✅ **年份和封面写入功能**：年份和封面都能正确写入
4. ✅ **API 调用异常处理**：所有 API 调用异常都能被捕获
5. ✅ **跳过逻辑**：找不到 `albummid` 时能正确跳过年份和封面处理
6. ✅ **独立处理逻辑**：音轨号、年份、封面能独立处理

### 预期的日志输出

```
[INFO] 处理文件 1/15: 01 B_O_K.ogg
[INFO] 写入音轨号 (OGG): 1  ← 关键验证点
[OK] 音轨号: 1  ← 关键验证点
[INFO] 查询 API 获取专辑信息...
[INFO] 找到 aDate: 2006-03-24  ← 关键验证点
[INFO] 下载封面: https://y.gtimg.cn/music/photo_new/T002R500x500M00000...jpg
[INFO] 封面数据大小: 52487 bytes  ← 关键验证点
[INFO] 处理文件 5/15: 05 情歌.flac
[INFO] 添加年份 (FLAC): 2006  ← 关键验证点
[INFO] 嵌入封面 (FLAC)  ← 关键验证点
[INFO] 保存元数据 (FLAC): 05 情歌.flac  ← 关键验证点
[OK] 05 情歌.flac
...
============================================================
处理完成
============================================================

总文件数: 15
成功: 15
失败: 0
跳过: 0
============================================================
```

---

## 📋 测试记录

### 测试 1：音轨号写入验证（⏸️ 待执行）

**执行时间**：-  
**执行人**：-  
**测试结果**：⏸️ 待测试

---

### 测试 2：音轨号检查验证（⏸️ 待执行）

**执行时间**：-  
**执行人**：-  
**测试结果**：⏸️ 待测试

---

### 测试 3：年份和封面写入验证（⏸️ 待执行）

**执行时间**：-  
**执行人**：-  
**测试结果**：⏸️ 待测试

---

### 测试 4：API 调用异常处理验证（⏸️ 待执行）

**执行时间**：-  
**执行人**：-  
**测试结果**：⏸️ 待测试

---

### 测试 5：找不到 albummid 跳过验证（⏸️ 待执行）

**执行时间**：-  
**执行人**：-  
**测试结果**：⏸️ 待测试

---

### 测试 6：批量处理验证（⏸️ 待执行）

**执行时间**：-  
**执行人**：-  
**测试结果**：⏸️ 待测试

---

## 🚀 下一步行动

### 等待实施方案确认

**待确认**：
1. ❓ **方案确认**：是否按照优化方案进行代码修改？
2. ❓ **测试确认**：是否按照测试方案进行功能测试？

### 如果确认实施

**我会执行**：
1. ✅ 新增 `write_track_number()` 函数（音轨号写入，不依赖 API）
2. ✅ 新增 `embed_year_and_cover()` 函数（年份和封面嵌入，依赖 API）
3. ✅ 修改主处理循环（拆分元数据处理）
4. ✅ 添加 API 调用异常处理（try-except 块）
5. ✅ 添加跳过逻辑（当找不到 `albummid` 时跳过年份和封面处理）
6. ✅ 创建最终报告文档

### 如果需要调整

**请告诉我**：
1. ❓ 需要调整实施方案吗？
2. ❓ 需要调整测试方案吗？
3. ❓ 需要添加其他功能吗？

---

## 📚 相关文档

- **优化方案**：`doc/元数据处理优化方案.md`（本文件）
- **OGG 音轨号写入实施方案**：`doc/OGG音轨号写入实施方案.md`（已实现）
- **OGG 音轨号写入测试方案**：`doc/OGG音轨号写入测试方案.md`（已实现）
- **API 调用异常修复方案**：`doc/API调用异常修复方案.md`（待评估，保留）
- **跳过逻辑修复方案**：`doc/跳过逻辑修复方案.md`（待评估，保留）
- **OGG 元数据支持文档**：`doc/OGG_METADATA_IMPLEMENTATION_RESULT.md`（已实现）

---

**文档创建时间**：2026-01-30  
**最后更新**：2026-01-30  
**维护者**：AI 助手
