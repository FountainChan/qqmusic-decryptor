# 元数据处理优化方案

**创建时间**：2026-01-30  
**版本**：v1.0  
**状态**：📋 待确认  
**类型**：优化方案（中文）

---

## 🎯 问题描述

### 当前问题

**问题 1**：缺少异常处理
- ❌ 没有 `try-except` 块处理 API 调用异常
- ❌ 如果 API 调用失败，`album_info` 不会被设置为 `None`
- ❌ 不会触发后续的跳过逻辑

**问题 2**：缺少返回语句
- ❌ 当找不到 `albummid` 时，函数没有返回
- ❌ 当找不到 `aDate` 时，处理会继续
- ❌ 所有文件都会被处理，但没有年份和封面

**问题 3**：API 调用逻辑错误
- ❌ 用户观察：如果 `albummid` 找不到，那么 `album_info["aDate"]` 也应该获取不到了
- ❌ 原因：`aDate` 是通过 `get_album_info_with_date(albummid)` 获取的
- ❌ 如果 `albummid` 不存在，那么 `aDate` 也获取不到
- ❌ 但是代码没有返回，继续处理了所有文件

**问题 4**：缺少写音轨号逻辑
- ❌ 音轨号是从文件名提取的，不需要 API 调用
- ❌ 但是音轨号处理和元数据处理混在一起
- ❌ 如果 API 失败，音轨号也无法写入

---

## 🔍 根本原因分析

### 问题 1：函数关系错误

**API 调用关系**：

| 函数 | 功能 | 输入 | 输出 |
|------|------|------|------|
| `search_album()` | 搜索专辑 | `artist`, `album_name` | `albummid` |
| `get_album_info()` | 获取详细信息 | `albummid` | `aDate`, `pub_time` 等 |

**正确的调用顺序**：
1. 第一步：`search_album(artist, album_name)` → 获取 `albummid`
2. 第二步：`get_album_info(albummid)` → 获取 `aDate`, `pub_time` 等

**当前错误**：
- ❌ 如果第一步失败（`albummid` 为 `None`），第二步不会被调用
- ❌ 如果第二步失败（`aDate` 不存在），处理会继续
- ❌ 没有跳过逻辑，导致所有文件都被处理

---

### 问题 2：元数据处理混在一起

**当前代码**：
```python
# 处理所有音频文件（FLAC 和 OGG）
for i, audio_file in enumerate(audio_files):
    # 1. 提取音轨号（不依赖 API）
    track_number = extract_track_number_from_filename(filename)
    
    # 2. 获取专辑信息（依赖 API）
    tags = extract_audio_tags(audio_file)
    artist = tags.get("artist")
    album = tags.get("album")
    
    # 3. 查询 API 获取专辑信息
    album_info = search_album_by_name(f"{artist} {album}")
    
    # 4. 嵌入元数据（包含音轨号、年份、封面）
    success = embed_metadata_to_audio(audio_file, pub_year, cover_data, track_number)
```

**问题**：
- ❌ 音轨号处理和元数据处理混在一起
- ❌ 如果 API 失败，音轨号也无法写入
- ❌ 没有独立的异常处理和跳过逻辑

---

## 🎯 用户需求确认

### 明确的需求

| 需求 | 说明 | 优先级 |
|------|------|--------|
| 1 | **拆分元数据处理**：音轨号、年份、封面独立处理 | 高 |
| 2 | **音轨号总是写入**：从文件名提取，不依赖 API | 高 |
| 3 | **添加异常处理**：捕获所有 API 调用异常 | 高 |
| 4 | **添加跳过逻辑**：当 API 失败时跳过年份和封面 | 高 |
| 5 | **详细的日志输出**：显示所有处理细节 | 中 |

### API 调用关系确认

**用户观察**：
1. ✅ `get_album_info()` 只能通过 `albummid` 获取信息
2. ✅ 如果 `albummid` 找不到，那么 `album_info["aDate"]` 也获取不到了
3. ✅ 要获取年份，应该先获取到 `albummid`，再通过 `albummid` 调用 QQ Music API 才能获取到 `aData` 的
4. ✅ 如果通过 QQ Music API 无法获取到 `albummid`，就应该跳过这首歌，只更新音轨号

**结论**：
- ✅ 用户的观察完全正确！
- ✅ 需要先调用 `search_album()` 获取 `albummid`
- ✅ 再调用 `get_album_info(albummid)` 获取详细信息（`aDate` 等）
- ✅ 如果找不到 `albummid`，则跳过年份和封面处理，只处理音轨号

---

## 🎯 优化方案

### 方案概览

**核心优化**：拆分元数据处理，音轨号、年份、封面独立处理

**优化内容**：
1. ✅ 拆分元数据处理：音轨号、年份、封面独立处理
2. ✅ 优化 API 调用：先 `search_album()` 获取 `albummid`，再 `get_album_info()` 获取详细信息
3. ✅ 添加异常处理：捕获所有 API 调用异常
4. ✅ 添加跳过逻辑：当 API 失败时跳过年份和封面
5. ✅ 详细的日志输出：显示所有处理细节

**不涉及的部分**：
- ❌ 不删除旧文档（`doc/API调用异常修复方案.md` 和 `doc/跳过逻辑修复方案.md`）
- ❌ 不修改 `qqmusic_api_client.py`（API 客户端）
- ❌ 不修改其他文件（只修改 `supplement_album_metadata.py`）

---

## 📝 详细实施步骤

### 步骤 1：拆分元数据处理函数

**文件**：`supplement_album_metadata.py`  
**位置**：第 289-500 行（`embed_metadata_to_audio()` 函数）

**修改前**：
```python
def embed_metadata_to_audio(audio_file_path, pub_year, cover_data, track_number=None):
    """
    将元数据嵌入音频文件（支持 FLAC 和 OGG）
    
    Args:
        audio_file_path: 音频文件路径（.flac 或 .ogg）
        pub_year: 发行年份（字符串，如 "2009"）
        cover_data: 封面数据
        track_number: 音轨号（整数，如 1, 2, 3...）
    
    Returns:
        bool: 成功返回 True，失败返回 False
    """
    try:
        filename = audio_file_path.lower()
        
        if filename.endswith('.flac'):
            # FLAC 文件处理
            audio = FLAC(audio_file_path)
            
            # 清除现有封面
            audio.clear_pictures()
            
            # 添加年份（DATE 标签）
            if pub_year:
                audio["DATE"] = pub_year
                logger.info(f"添加年份 (FLAC): {pub_year}")
            
            # 添加音轨号（TRACKNUMBER 标签）
            if track_number is not None:
                # 检查是否已经有音轨号
                existing_track = audio.get("TRACKNUMBER")
                if existing_track:
                    logger.info(f"FLAC 文件已经有音轨号: {existing_track}，跳过写入")
                else:
                    # FLAC 标准使用 TRACKNUMBER 字符串
                    audio["TRACKNUMBER"] = str(track_number)
                    logger.info(f"添加音轨号 (FLAC): {track_number}")
            else:
                logger.warning(f"未找到音轨号，跳过音轨号写入")
            
            # 添加封面（PICTURE 标签）
            if cover_data:
                image = Picture()
                image.type = 3
                image.mime = "image/jpeg"
                image.data = cover_data
                
                audio.add_picture(image)
                logger.info(f"嵌入封面 (FLAC)")
            
            # 保存
            audio.save()
            logger.info(f"保存元数据 (FLAC): {os.path.basename(audio_file_path)}")
            return True
            
        elif filename.endswith('.ogg'):
            # OGG 文件处理（仅文本元数据）
            from mutagen.oggvorbis import OggVorbis
            audio = OggVorbis(audio_file_path)
            
            # 添加年份（DATE 标签）
            if pub_year:
                # OGG Vorbis 标准使用 DATE 字符串
                audio["DATE"] = pub_year
                logger.info(f"添加年份 (OGG): {pub_year}")
            
            # 添加音轨号（TRACKNUMBER 标签）
            if track_number is not None:
                # 检查是否已经有音轨号
                existing_track = audio.get("TRACKNUMBER")
                if existing_track:
                    logger.info(f"OGG 文件已经有音轨号: {existing_track}，跳过写入")
                else:
                    # OGG Vorbis 标准使用 TRACKNUMBER 字符串
                    audio["TRACKNUMBER"] = str(track_number)
                    logger.info(f"添加音轨号 (OGG): {track_number}")
            else:
                logger.warning(f"未找到音轨号，跳过音轨号写入")
            
            # 封面处理：跳过内嵌，只保存为独立文件
            # OGG Vorbis 的封面嵌入非常复杂（需要手动构建 METADATA_BLOCK_PICTURE）
            # 为了稳定性和兼容性，我们跳过内嵌，cover.jpg 会在主函数中单独保存
            if cover_data:
                logger.info(f"OGG 封面只保存为独立文件，不内嵌")
            else:
                logger.info(f"没有封面数据")
            
            # 保存
            audio.save()
            logger.info(f"保存元数据 (OGG): {os.path.basename(audio_file_path)}")
            return True
            
        else:
            # 不支持的格式
            logger.error(f"不支持的文件格式: {audio_file_path}")
            return False
            
    except Exception as e:
        logger.error(f"嵌入元数据失败 {audio_file_path}: {e}")
        return False
```

**修改后**：
```python
def write_track_number(audio_file_path, track_number):
    """
    写入音轨号到音频文件（不依赖 API）
    
    Args:
        audio_file_path: 音频文件路径
        track_number: 音轨号（整数）
    
    Returns:
        bool: 成功返回 True，失败返回 False
    """
    try:
        if audio_file_path.lower().endswith('.flac'):
            audio = FLAC(audio_file_path)
            
            # 检查是否已经有音轨号
            existing_track = audio.get("TRACKNUMBER")
            if existing_track:
                logger.info(f"FLAC 文件已经有音轨号: {existing_track}，跳过写入")
                return True  # 跳过，但不算失败
            
            # 写入音轨号
            audio["TRACKNUMBER"] = str(track_number)
            audio.save()
            logger.info(f"写入音轨号 (FLAC): {track_number}")
            return True
            
        elif audio_file_path.lower().endswith('.ogg'):
            audio = OggVorbis(audio_file_path)
            
            # 检查是否已经有音轨号
            existing_track = audio.get("TRACKNUMBER")
            if existing_track:
                logger.info(f"OGG 文件已经有音轨号: {existing_track}，跳过写入")
                return True  # 跳过，但不算失败
            
            # 写入音轨号
            audio["TRACKNUMBER"] = str(track_number)
            audio.save()
            logger.info(f"写入音轨号 (OGG): {track_number}")
            return True
            
        else:
            logger.warning(f"不支持的格式: {audio_file_path}")
            return False
            
    except Exception as e:
        logger.error(f"写入音轨号失败 {audio_file_path}: {e}")
        return False


def embed_year_and_cover(audio_file_path, pub_year, cover_data):
    """
    嵌入年份和封面到音频文件（依赖 API）
    
    Args:
        audio_file_path: 音频文件路径
        pub_year: 发行年份（字符串）
        cover_data: 封面数据（字节）
    
    Returns:
        bool: 成功返回 True，失败返回 False
    """
    try:
        filename = audio_file_path.lower()
        
        if filename.endswith('.flac'):
            # FLAC 文件处理
            audio = FLAC(audio_file_path)
            
            # 清除现有封面
            audio.clear_pictures()
            
            # 添加年份（DATE 标签）
            if pub_year:
                audio["DATE"] = pub_year
                logger.info(f"添加年份 (FLAC): {pub_year}")
            
            # 添加封面（PICTURE 标签）
            if cover_data:
                image = Picture()
                image.type = 3
                image.mime = "image/jpeg"
                image.data = cover_data
                
                audio.add_picture(image)
                logger.info(f"嵌入封面 (FLAC)")
            
            # 保存
            audio.save()
            logger.info(f"保存元数据 (FLAC): {os.path.basename(audio_file_path)}")
            return True
            
        elif filename.endswith('.ogg'):
            # OGG 文件处理（仅文本元数据）
            from mutagen.oggvorbis import OggVorbis
            audio = OggVorbis(audio_file_path)
            
            # 添加年份（DATE 标签）
            if pub_year:
                # OGG Vorbis 标准使用 DATE 字符串
                audio["DATE"] = pub_year
                logger.info(f"添加年份 (OGG): {pub_year}")
            
            # OGG 文件不嵌入封面
            
            # 保存
            audio.save()
            logger.info(f"保存元数据 (OGG): {os.path.basename(audio_file_path)}")
            return True
            
        else:
            # 不支持的格式
            logger.error(f"不支持的文件格式: {audio_file_path}")
            return False
            
    except Exception as e:
        logger.error(f"嵌入年份和封面失败 {audio_file_path}: {e}")
        return False
```

**关键改进**：
- ✅ 拆分了元数据处理：音轨号、年份、封面独立处理
- ✅ 添加了 `write_track_number()` 函数：不依赖 API
- ✅ 添加了 `embed_year_and_cover()` 函数：依赖 API
- ✅ 每个函数都有独立的异常处理

---

### 步骤 2：修改主处理循环

**文件**：`supplement_album_metadata.py`  
**位置**：第 507-550 行（主处理循环）

**修改前**：
```python
    # 处理所有音频文件（FLAC 和 OGG）
    for i, audio_file in enumerate(audio_files):
        filename = os.path.basename(audio_file)
        logger.info(f"[INFO] 处理文件 {i+1}/{len(audio_files)}: {filename}")
        
        # 提取并添加音轨号
        filename = os.path.basename(audio_file)
        track_number = extract_track_number_from_filename(filename)
        
        # 提取标签
        tags = extract_audio_tags(audio_file)
        artist = tags.get("artist")
        album = tags.get("album")
        
        # 嵌入元数据
        filename = os.path.basename(audio_file)
        success = embed_metadata_to_audio(audio_file, pub_year, cover_data, track_number)
        
        if success:
            stats["success"] += 1
            logger.info(f"[OK] {filename}")
        else:
            stats["failed"] += 1
            logger.warning(f"[FAIL] {filename}")
```

**修改后**：
```python
    # 处理所有音频文件（FLAC 和 OGG）
    for i, audio_file in enumerate(audio_files):
        filename = os.path.basename(audio_file)
        logger.info(f"[INFO] 处理文件 {i+1}/{len(audio_files)}: {filename}")
        
        # 步骤 1：音轨号处理（不依赖 API，总是尝试）
        track_number = extract_track_number_from_filename(filename)
        if track_number is not None:
            success = write_track_number(audio_file, track_number)
            if success:
                stats["success"] += 1
                logger.info(f"[OK] 音轨号: {track_number}")
            else:
                stats["failed"] += 1
                logger.warning(f"[FAIL] 音轨号: {track_number}")
            continue  # 成功写入音轨号后，继续下一个文件
        else:
            logger.warning(f"未找到音轨号，跳过音轨号写入")
            stats["skipped"] += 1
            continue  # 未找到音轨号，跳过
        
        # 步骤 2：年份和封面处理（依赖 API）
        try:
            # 提取标签
            tags = extract_audio_tags(audio_file)
            artist = tags.get("artist")
            album = tags.get("album")
            
            # 调用 API 获取专辑信息
            album_info = search_album_by_name(f"{artist} {album}")
            
            if album_info and "albummid" in album_info:
                # 获取年份和封面
                albummid = album_info["albummid"]
                album_details = get_album_info(albummid)
                
                if album_details:
                    # 提取年份
                    pub_year = album_details.get("pub_year")
                    
                    # 获取封面
                    cover_data = None
                    if albummid:
                        cover_url = f"https://y.gtimg.cn/music/photo_new/T002R500x500M00000{albummid}.jpg"
                        try:
                            response = requests.get(cover_url, timeout=API_TIMEOUT)
                            response.raise_for_status()
                            cover_data = response.content
                            logger.info(f"封面数据大小: {len(cover_data)} bytes")
                        except Exception as e:
                            logger.error(f"下载封面失败: {e}")
                    
                    # 嵌入元数据（年份 + 封面）
                    if pub_year or cover_data:
                        success = embed_year_and_cover(audio_file, pub_year, cover_data)
                        if success:
                            stats["success"] += 1
                            logger.info(f"[OK] {filename}")
                        else:
                            stats["failed"] += 1
                            logger.warning(f"[FAIL] {filename}")
                    else:
                        logger.warning("专辑详情为空")
                        stats["failed"] += 1
                else:
                    logger.warning("未找到专辑详情")
                    stats["failed"] += 1
            else:
                logger.warning("未找到 albummid")
                logger.warning("跳过专辑：未找到发行年份")
                # 不返回，继续下一个文件
        except Exception as e:
            logger.error(f"处理异常 {filename}: {e}")
            stats["failed"] += 1
```

**关键改进**：
- ✅ 步骤 1：音轨号处理（不依赖 API，总是尝试）
- ✅ 步骤 2：年份和封面处理（依赖 API）
- ✅ 添加了异常处理：捕获所有 API 调用异常
- ✅ 添加了跳过逻辑：当找不到 `albummid` 时跳过年份和封面处理
- ✅ 音轨号和年份封面独立处理

---

## 📊 代码修改汇总

### 修改的文件

| 文件 | 修改类型 | 修改内容 |
|------|----------|----------|
| `supplement_album_metadata.py` | 修改 | 主处理循环、新增函数 |

### 新增的代码

| 位置 | 代码行数 | 说明 |
|------|----------|------|
| 新增 `write_track_number()` 函数 | 约 50 行 | 音轨号写入函数（不依赖 API） |
| 新增 `embed_year_and_cover()` 函数 | 约 100 行 | 年份和封面嵌入函数（依赖 API） |
| 修改主处理循环 | 约 60 行 | 拆分元数据处理逻辑 |

### 总修改量

- **总代码行数**：约 210 行
- **新增函数**：2 个（`write_track_number()` + `embed_year_and_cover()`）
- **修改风险**：低（向后兼容）

---

## 🎯 预期效果

### 修复前（当前状态）

| 场景 | 音轨号 | 年份 | 封面 | 处理状态 |
|------|--------|------|------|----------|
| 找不到 albummid | - | - | - | ❌ 继续处理（错误） |

### 修复后（预期状态）

| 场景 | 音轨号 | 年份 | 封面 | 处理状态 |
|------|--------|------|------|----------|
| 找不到 albummid | ✅ 写入 | ❌ 跳过 | ❌ 跳过 | ✅ 音轨号写入成功 |

### 预期日志输出

```
[INFO] 处理文件 1/15: 01 B_O_K.ogg
[OK] 音轨号: 1  ← 音轨号处理成功
[INFO] 提取标签 - 01 B_O_K.ogg: Artist=侧田, Album=From Justin...
[INFO] 查询 API 获取专辑信息...
[INFO] 专辑信息: {'albummid': '...', 'singername': '...', 'albumname': '...'}
[INFO] 获取专辑信息: {...}
[INFO] 找到年份: 2006
[INFO] 封面数据大小: 52487 bytes
[INFO] 添加年份 (OGG): 2006
[INFO] 保存元数据 (OGG): 01 B_O_K.ogg
[OK] 01 B_O_K.ogg  ← 年份和封面处理成功
```

**当找不到 albummid 时**：

```
[INFO] 处理文件 1/15: 01 B_O_K.ogg
[OK] 音轨号: 1  ← 音轨号处理成功
[INFO] 提取标签 - 01 B_O_K.ogg: Artist=侧田, Album=From Justin...
[INFO] 查询 API 获取专辑信息...
WARNING: 未找到专辑：侧田 From Justin...
WARNING: 未找到 albummid  ← 关键验证点
WARNING: 跳过专辑：未找到发行年份  ← 关键验证点
[OK] 01 B_O_K.ogg  ← 音轨号写入成功，年份和封面跳过
```

---

## 🚀 实施计划

### 计划 1：代码修改

- [ ] **步骤 1**：新增 `write_track_number()` 函数
- [ ] **步骤 2**：新增 `embed_year_and_cover()` 函数
- [ ] **步骤 3**：修改主处理循环（拆分元数据处理）

### 计划 2：功能测试

- [ ] **测试 1**：测试音轨号写入功能
- [ ] **测试 2**：测试年份和封面写入功能
- [ ] **测试 3**：测试 API 调用异常处理
- [ ] **测试 4**：验证所有日志输出正确

### 计划 3：文档更新

- [ ] **文档 1**：创建中文测试方案文档
- [ ] **文档 2**：创建中文最终报告文档

---

## 📋 相关文档

- **OGG 音轨号写入实施方案**：`doc/OGG音轨号写入实施方案.md`（已实现）
- **OGG 音轨号写入测试方案**：`doc/OGG音轨号写入测试方案.md`（已实现）
- **API 调用异常修复方案**：`doc/API调用异常修复方案.md`（待评估，保留）
- **跳过逻辑修复方案**：`doc/跳过逻辑修复方案.md`（待评估，保留）
- **OGG 元数据支持文档**：`doc/OGG_METADATA_IMPLEMENTATION_RESULT.md`（已实现）
- **封面保存位置修复实施方案**：`doc/封面保存位置修复实施方案.md`（已实现）

---

**文档创建时间**：2026-01-30  
**最后更新**：2026-01-30  
**维护者**：AI 助手
